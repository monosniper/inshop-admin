import { defaultTheme } from 'react-admin';

export const theme = {
    ...defaultTheme,
    palette: {
        secondary: {
            main: "#282828",
            primary: '#EB00E1'
        },
    },
};