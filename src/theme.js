import { defaultTheme } from 'react-admin';

const theme = {
    ...defaultTheme,
    palette: {
        secondary: {
            main: "#282828",
            primary: '#EB00E1'
        },
    },
};

export default theme